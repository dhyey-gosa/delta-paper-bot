services:
  - type: web
    name: delta-paper-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python paper_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    healthCheckPath: /health
